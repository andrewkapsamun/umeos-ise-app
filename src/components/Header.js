import React from 'react';

const Header = () => {
    return (
        <nav className="navbar navbar-dark bg-dark">
            <a className="navbar-brand" href="/">Информационнные системы в экономике</a>
        </nav>
    );
};

export default Header;